import { createAction } from '@ngrx/store'

export const increment = createAction('[Product Component] Increment');
export const decrement = createAction('[Product Component] Decrement');
export const reset = createAction('[Product Component] Reset');
